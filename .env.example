# CORTEX Environment Configuration Template
# Copy this file to .env and configure values locally.
# NEVER commit .env to version control.

# GitHub API Token (Optional, for repository integration)
GITHUB_TOKEN=
